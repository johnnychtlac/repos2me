其核心功能是支持并行分页（parallel-paging）。它允许用户在文档中创建左右并排的列（平行页），并且这些列可以独立分页，常用于对照翻译、双语排版或需要左右对比内容的场景。除了基本的双栏/多栏布局外，它还具备以下高级特性：支持在并行页环境内外为页面或特定区域绘制背景颜色或图案。能够处理跨栏或特定栏内的浮动体（如图片、表格）。提供了专门的机制来处理被分割到不同页面的脚注（通过 \footnotesep 等参数控制）。支持在数学模式和文本模式下进行着色，并能正确处理颜色栈（color stack）的推送和弹出，避免颜色泄露。