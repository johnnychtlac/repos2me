array 宏包是对 LaTeX 标准 tabular 和 array 环境的扩展实现，旨在提供更强大的表格排版功能，并解决原生环境的一些局限性。允许用户在定义列格式时直接插入声明（如字体、数学模式切换），而无需在每一行的每个单元格中重复输入。在 tabular 和 array 环境中使用脆弱命令（fragile commands）时，不再需要手动添加 \protect。垂直线和水平线的处理方式与标准 LaTeX 不同，线条宽度会额外增加表格的总宽度或高度，避免了线条重叠或间距不当的问题。
