这是一个基于LuaLaTeX和MetaPost开发的工具，专门用于在LaTeX文档中生成词云（Wordcloud）。与常见的图片式词云不同，这个宏包生成的词云具有矢量图形的特性，且支持直接渲染LaTeX数学公式，非常适合学术排版。你可以直接在词云中插入LaTeX数学公式（如\LaTeX，\TeX），这是目前其他词云工具很难做到的。由于需要逐个检测单词间的碰撞（Intersection），计算量较大，生成速度较慢（尤其是单词数量多时）。目前仅基于矩形边界框检测，而非字母的实际轮廓（后者计算太慢），因此单词间可能会留有较多空隙。