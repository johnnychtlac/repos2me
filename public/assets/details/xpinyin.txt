xpinyin是一个专为LaTeX设计的宏包，其核心功能是为汉字自动添加拼音（注音）。它可以自动识别汉字并为其上方添加对应的汉语拼音。内置了基于UnicodeUnihan数据库的拼音数据。支持用户手动指定多音字的读音（例如区分“长”字的读音）。可以通过设置高亮显示多音字，方便用户校对。可以独立设置拼音的字体（默认与正文西文字体相同）和相对于正文的大小比例（默认0.4）。支持调整拼音与汉字之间的垂直距离、汉字之间的水平距离以及拼音之间的间隔。支持设置拼音的颜色、格式等。