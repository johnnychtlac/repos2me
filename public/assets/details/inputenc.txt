主要用于指定和处理文档源代码的输入编码。它允许用户在文档中直接使用特定字符集（如 ASCII、ISO Latin-1、Windows-1252、UTF-8 等）中的特殊字符（如重音符号、货币符号等），而无需全部使用 LaTeX 命令（如 \'{e}）来输入。inputenc 是传统 pdfLaTeX 工作流中处理非 ASCII 字符的关键桥梁，它让用户能直接用键盘输入带重音的字母或特殊符号，而无需记忆繁琐的 LaTeX 转义命令。但在现代 LaTeX 排版中，推荐优先使用 UTF-8 编码 配合 XeLaTeX 或 LuaLaTeX 引擎，从而摆脱对该宏包的依赖。