该宏包旨在改进 LaTeX 中段落构建的机制，解决传统 TeX 引擎在处理段落开始和结束时的局限性。主要目标包括：为自动标记 PDF 文档结构提供支持，这对于无障碍访问（Accessibility）至关重要。宏包定义了四个在每個段落处理过程中执行的公共钩子，允许用户和宏包在段落处理的关键阶段安全地插入代码，而无需直接修改底层 TeX 原语（如 \everypar 或 \par），从而避免不同宏包之间的冲突。除了公共钩子外，还定义了四个内部内核钩子（如 \@kernel@before@para@begin 等），主要用于内核实现 PDF 标签化功能，通常不建议用户直接修改。