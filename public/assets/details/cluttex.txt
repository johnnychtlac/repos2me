ClutTEX 是一个专为 LaTeX 文档处理设计的自动化工具。它的核心理念是“不污染工作目录”，旨在通过隔离辅助文件来保持项目文件夹的整洁，同时自动处理复杂的编译流程（如多次运行、索引生成等）。它解决了 LaTeX 编译时产生大量中间文件（如 .aux，.log，.toc 等）导致项目目录混乱的问题。所有辅助文件默认被创建在系统的临时目录（Temporary Directory）中，而不是你的源文件旁边。你可以指定一个输出目录（--output-directory），将所有生成的文件集中管理，保持源码目录干净。